# Ignore everything in this directory by default so a plaintext
# secret can never be committed by accident. Only .age ciphertext
# (and this file) are allowed through.
*
!.gitignore
!README.md
!*.age
